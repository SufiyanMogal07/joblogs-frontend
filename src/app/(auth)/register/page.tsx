import RegisterForm from "@/components/features/auth/RegisterForm";

const page = () => {
  return (
    <div className="min-h-dvh md:min-h-screen flex flex-col items-center justify-center bg-slate-950">
      <RegisterForm />
    </div>
  );
};

export default page;
